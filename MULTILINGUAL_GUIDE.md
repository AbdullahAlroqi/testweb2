# 🌍 دليل الترجمة متعددة اللغات

## نظام الترجمة الجديد

تم إضافة نظام ترجمة متعدد اللغات لصفحات الموظفين يدعم:
- 🇸🇦 **العربية** (Arabic)
- 🇬🇧 **الإنجليزية** (English)  
- 🇧🇩 **البنغالية** (Bengali/Bangladeshi)

---

## 🎯 كيفية الاستخدام

### للموظف:

1. سجل دخول كموظف
2. في أعلى أي صفحة من صفحات الموظف، ستجد 3 أزرار:
   - 🇸🇦 **عربي**
   - 🇬🇧 **English**
   - 🇧🇩 **বাংলা**

3. اضغط على اللغة التي تريدها
4. ✅ ستتغير جميع النصوص فوراً!

---

## 📄 الصفحات المترجمة

### ✅ لوحة تحكم الموظف (`/employee/dashboard`)
- الترحيب والعنوان
- عمليات اليوم
- البحث عن عميل
- التحقق من كوبون

### ✅ البحث عن عميل (`/employee/search`)
- عنوان البحث
- حقل البحث
- جدول النتائج
- الأزرار

### 🔜 قريباً:
- صفحة عرض العميل
- صفحة التحقق من الكوبون

---

## 🔤 الترجمات المتوفرة

### العربية (الافتراضية):
```
- لوحة التحكم
- البحث عن عميل
- التحقق من كوبون
- إضافة كوب جديد
- صرف الكوب المجاني
```

### English:
```
- Dashboard
- Search Customer
- Verify Coupon
- Add New Cup
- Redeem Free Cup
```

### বাংলা (Bengali):
```
- ড্যাশবোর্ড (Dashboard)
- গ্রাহক খুঁজুন (Search Customer)
- কুপন যাচাই করুন (Verify Coupon)
- নতুন কাপ যোগ করুন (Add New Cup)
- বিনামূল্যে কাপ নিন (Redeem Free Cup)
```

---

## 💡 مميزات النظام

### ✅ تلقائي:
- اللغة تُحفظ في الجلسة
- تبقى اللغة المختارة حتى تغييرها
- تعمل على جميع صفحات الموظف

### ✅ سهل:
- زر واحد لتغيير اللغة
- لا حاجة لإعادة تسجيل الدخول
- لا حاجة لإعادة تحميل الصفحة يدوياً

### ✅ واضح:
- الزر النشط بلون مختلف
- علم الدولة لكل لغة
- ترجمة احترافية

---

## 🔧 للمطورين

### إضافة ترجمة جديدة:

1. افتح `translations.py`
2. أضف الترجمات في قاموس `TRANSLATIONS`
3. مثال:

```python
'new_text': {
    'ar': 'نص جديد',
    'en': 'New Text',
    'bn': 'নতুন পাঠ্য'
}
```

### استخدام الترجمة في القالب:

```html
<h1>{{ t.new_text }}</h1>
```

### إضافة لغة جديدة:

1. في `translations.py`، أضف قاموس جديد:
```python
'ur': {  # Urdu مثلاً
    'dashboard': 'ڈیش بورڈ',
    'search': 'تلاش',
    # ...
}
```

2. في `app.py`، عدّل route تغيير اللغة:
```python
if lang in ['ar', 'en', 'bn', 'ur']:
```

3. في القالب، أضف زر اللغة:
```html
<a href="{{ url_for('change_language', lang='ur') }}" class="btn btn-sm">
    🇵🇰 اردو
</a>
```

---

## 📊 إحصائيات الترجمة

| اللغة | المفاتيح المترجمة | النسبة المئوية |
|------|------------------|----------------|
| العربية 🇸🇦 | 30+ | 100% ✅ |
| English 🇬🇧 | 30+ | 100% ✅ |
| বাংলা 🇧🇩 | 30+ | 100% ✅ |

---

## 🎬 أمثلة عملية

### مثال 1: موظف بنغالي

```
الموظف: রহিম (Rahim)
اللغة الأم: Bengali

1. يسجل دخول
2. يضغط 🇧🇩 বাংলা
3. يرى:
   ড্যাশবোর্ড (Dashboard)
   গ্রাহক খুঁজুন (Search Customer)
4. يبحث عن গ্রাহক (Customer)
5. يضغط দেখুন (View)
6. يضيف নতুন কাপ (New Cup)
```

### مثال 2: موظف إنجليزي

```
Employee: John
Native Language: English

1. Logs in
2. Clicks 🇬🇧 English
3. Sees:
   Dashboard
   Search Customer
4. Searches for "Ahmed"
5. Clicks "View"
6. Adds "New Cup"
```

---

## ⚙️ الملفات المعدلة

```
New:
- translations.py           # ملف الترجمات

Modified:
- app.py                    # إضافة دعم اللغات
- templates/employee/dashboard.html     # أزرار اللغة
- templates/employee/search.html        # أزرار اللغة
```

---

## 🐛 حل المشاكل

### المشكلة: اللغة لا تتغير
**الحل:** امسح الـ cache وأعد تحميل الصفحة (Ctrl+F5)

### المشكلة: بعض النصوص لا تترجم
**الحل:** تأكد من وجود المفتاح في `translations.py`

### المشكلة: الترجمة البنغالية لا تظهر بشكل صحيح
**الحل:** تأكد من دعم UTF-8 في المتصفح

---

## 🌟 مميزات قادمة

- [ ] ترجمة صفحة عرض العميل
- [ ] ترجمة صفحة التحقق من الكوبون
- [ ] حفظ تفضيل اللغة في قاعدة البيانات
- [ ] ترجمة رسائل الـ Flash
- [ ] إضافة لغات إضافية (Urdu, Hindi, etc.)
- [ ] ترجمة صفحات المدير والعميل

---

## 📞 الدعم

إذا واجهت مشكلة في الترجمة:
1. راجع `translations.py` للمفاتيح المتاحة
2. تحقق من `app.py` للـ route الصحيح
3. جرب على متصفح مختلف

---

**استمتع بالنظام متعدد اللغات! 🌍✨**

## نصيحة للمديرين:
إذا كان لديك موظفون من جنسيات مختلفة، هذه الميزة ستسهل عملهم بشكل كبير ومباشر!
